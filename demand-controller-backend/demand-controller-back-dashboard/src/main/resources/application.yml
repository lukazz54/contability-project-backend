server:
  port: 8080
  
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: contability
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
    static-path-pattern: /swagger*